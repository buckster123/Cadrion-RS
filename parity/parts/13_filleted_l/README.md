# 13 — filleted L (OCCT-only)

Two-plate L-bracket (`union` of horiz + vert plates) + all-edge fillet.

**Mock:** fillet → `CADRION-E-UNSUPPORTED` — no `expect.json`.
**OCCT:** `expect.occt.json` in suite `fillet-occt` (H3-7 hard golden).
