[package]
name = "cadre"
version = "0.1.0"
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "Cadre-RS facade — re-exports core crates"
# crates.io `cadre` is Modal's archived config crate (OQ-1 / docs/NAME_OQ1.md).
publish = false

[dependencies]
cadre-kernel = { workspace = true }
cadre-lang = { workspace = true }
cadre-model = { workspace = true }
cadre-inspect = { workspace = true }
